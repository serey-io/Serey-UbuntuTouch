# Assets

`logo.svg` is a placeholder app icon. Replace it with the official Serey brand
icon (keep the same filename, or update the `Icon=` line in `serey.desktop` and
`ICON_FILE` in `CMakeLists.txt`). A 256×256 (or larger) square icon is expected.
